package core

import chisel3._

// ===========================================================================
// CPU 配置常量
// ===========================================================================

object Config {
  /** CPU 复位后从哪个地址开始取指 (匹配 ysyxSoC) */
  val START_ADDR: BigInt = BigInt("2ffffffc", 16)

  /** 数据位宽 */
  val XLEN = 32
}
